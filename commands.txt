docker build -t trimesh-env .

v1
docker run -it --rm -v $(pwd)/v1:/workspace/code -v $(pwd)/obj:/workspace/obj -v $(pwd)/plots:/workspace/plots -v $(pwd)/output:/workspace/output trimesh-env
v2
docker run -it --rm -v $(pwd)/v2:/workspace/code -v $(pwd)/obj:/workspace/obj trimesh-env

docker buildx prune -f
docker image prune -f

